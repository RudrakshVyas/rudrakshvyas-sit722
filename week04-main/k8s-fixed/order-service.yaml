apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: sit722
  labels:
    app: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: order-service
    spec:
      restartPolicy: Always
      containers:
        - name: order-service
          image: docker.io/rudrakshvyas/order-microservice:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          envFrom:
            - configMapRef:
                name: config-map
            - secretRef:
                name: secrets
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: sit722
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8000
  type: ClusterIP
