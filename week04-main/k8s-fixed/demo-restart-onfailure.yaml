apiVersion: v1
kind: Pod
metadata:
  name: demo-restart-onfailure
  namespace: sit722
spec:
  restartPolicy: OnFailure
  containers:
    - name: busybox
      image: busybox
      command: ["sh","-c","echo 'This will exit with 1' && exit 1"]
