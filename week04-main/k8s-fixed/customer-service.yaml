apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  namespace: sit722
  labels:
    app: customer-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: customer-service
    spec:
      restartPolicy: Always
      containers:
        - name: customer-service
          image: docker.io/rudrakshvyas/customer-microservice:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          envFrom:
            - configMapRef:
                name: config-map
            - secretRef:
                name: secrets
---
apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: sit722
  labels:
    app: customer-service
spec:
  selector:
    app: customer-service
  ports:
    - name: http
      protocol: TCP
      port: 8002
      targetPort: 8000
  type: ClusterIP
